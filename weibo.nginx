server {
    listen 80;
    # location / {
    #    proxy_pass http://0.0.0.0:5000;
    # }
    root /var/www/express-mongodb-boilerplate/dist;


    location / {
        #转发任何请求到 index.html
        index index.html;
    }
}
